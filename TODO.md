# TODO: Fix JWT Build Error

- [x] Update package.json to add 'jsonwebtoken' to dependencies and '@types/jsonwebtoken' to devDependencies
- [x] Run npm install to install the new dependencies
- [x] Run npm run build to verify the fix
